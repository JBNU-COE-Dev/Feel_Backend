services:
  # Nginx Reverse Proxy (HTTP only - IP 배포용)
  nginx:
    image: nginx:alpine
    container_name: feel-nginx
    ports:
      - "80:80"
      # HTTPS 포트는 도메인 발급 후 활성화
      # - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      # SSL 인증서 볼륨은 도메인 발급 후 활성화
      # - ./certbot/conf:/etc/letsencrypt:ro
      # - ./certbot/www:/var/www/certbot:ro
    depends_on:
      - backend
    networks:
      - feel-network
    restart: unless-stopped
    command: "/bin/sh -c 'while :; do sleep 6h & wait $${!}; nginx -s reload; done & nginx -g \"daemon off;\"'"

  # Certbot for SSL Certificate (도메인 발급 후 활성화)
  # certbot:
  #   image: certbot/certbot
  #   container_name: feel-certbot
  #   volumes:
  #     - ./certbot/conf:/etc/letsencrypt
  #     - ./certbot/www:/var/www/certbot
  #   entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  # Spring Boot Backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feel-backend
    expose:
      - "8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_USERNAME=${DB_USERNAME:-root}
      - DB_PASSWORD=${DB_PASSWORD:-password}
    depends_on:
      - db
    networks:
      - feel-network
    restart: unless-stopped

  # MySQL Database (배포용)
  db:
    image: mysql:8.0
    container_name: feel-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-password}
      MYSQL_DATABASE: feeldb
      MYSQL_USER: ${DB_USERNAME:-feel}
      MYSQL_PASSWORD: ${DB_PASSWORD:-password}
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - feel-network
    restart: unless-stopped

networks:
  feel-network:
    driver: bridge

volumes:
  mysql-data:
